import React from 'react';
import bplData from '../../bplData.json';




const Players = () => {
    console .log(bplData);
    // useEffect(() => {
    //    console.log(bplData);
    // }, [])

    return (
        
        <div>
            <h1>hello pelyers</h1>
        </div>
    );
};

export default Players;